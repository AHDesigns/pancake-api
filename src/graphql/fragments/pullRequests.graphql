# import '../fragments/reviews.graphql'

fragment pullRequests on Repository {
    pullRequests(first: $prCount, states: [OPEN]) {
        nodes {
            author {
                login
            }
            title
            ...reviews
        }
    }
}
