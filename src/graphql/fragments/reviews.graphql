fragment reviews on PullRequest {
    reviews(first: $reviewsCount) {
        nodes {
            author {
                login
            }
            createdAt
            state
        }
    }
}
