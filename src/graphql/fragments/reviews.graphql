fragment reviews on PullRequest {
    reviews(first: $reviewsCount, states: [CHANGES_REQUESTED, APPROVED]) {
        nodes {
            author {
                login
            }
            createdAt
            state
        }
    }
}
