query {
    repository(name: "skymobile-service", owner: "sky-uk") {
        projects (first: 5){
            nodes {
                name
                columns (first: 15) {
                    nodes {
                        name
                        id
                    }
                }
            }
        }
    }
}
